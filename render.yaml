services:
  - type: web
    name: gae-rj
    env: python
    buildCommand: |
      pip install -r requirements.txt
    startCommand: |
      python manage.py migrate &&
      echo "from estrutura.models import Usuario; Usuario.objects.filter(username='joao.rafael').exists() or Usuario.objects.create_superuser('joao.rafael', 'admin@email.com', 'Zetraxa4@#')" | python manage.py shell &&
      gunicorn core.wsgi:application
    envVars:
      - key: SECRET_KEY
        sync: false
